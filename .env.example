# cp .env.example .env
#
# 這四個是 Portal 的全部部署設定，而且沒有一個是機密：client_id 會明文出現在授權
# 請求的網址上，其餘都是網址。Portal 是 Public client（沒有 client secret），
# 靠 PKCE 保護 —— 瀏覽器裡藏不住密鑰，所以本來就不該有。
#
# 產品目錄不在這裡 —— 見 src/products.ts。本機開發讀這個 .env；跑在容器裡時改由 entrypoint 在啟動當下注入
# （見 docker-entrypoint.d/40-generate-env-config.sh）。兩條路徑都只經過
# src/config/runtimeEnv.ts 的 getEnv()。

# Authentik 這個 application 的 issuer。endpoint 全部由
# <authority>/.well-known/openid-configuration 取得，不用逐一設定。
# 這個值必須「逐字」等於 discovery 回傳的 issuer —— Authentik 的 issuer 是從
# 請求的 Host 標頭算出來的，換 host 就是換 issuer，而換 issuer 會讓已綁定的
# (issuer, subject) 安靜地變成孤兒：症狀不是壞掉，是這個人變成一個沒見過的新使用者。
VITE_AUTHENTIK_AUTHORITY=http://localhost:9000/application/o/your-app-slug/

VITE_AUTHENTIK_CLIENT_ID=your-client-id

# 登入完成後導回的位址，以及登出後導回的位址。兩個都是首頁 ——
# 這個 app 只有一個畫面，oidc-client-ts 偵測到網址帶 code/state 就會自動完成交換。
#
# 注意：Authentik 對這兩個值都是「字串完全比對」（Provider 的 Redirect URIs 裡
# matching mode=strict 的那筆），連結尾斜線都算不同。這裡帶尾斜線，Authentik
# 那邊就要註冊成一模一樣的值，否則登入／登出會被擋成 invalid_request。
VITE_AUTHENTIK_REDIRECT_URI=http://localhost:6030/
VITE_AUTHENTIK_POST_LOGOUT_REDIRECT_URI=http://localhost:6030/

# 首頁要列出的產品不在這裡 —— 產品目錄對所有部署都一樣，屬於程式碼，
# 見 src/products.ts。誰能看到哪些產品則由 Authentik 的授權決定。
